{
  "license": "MIT",
  "scripts": {
    "dev": "~/.okta/aws-token && node -r @babel/register -r dotenv/config src/express.js",
    "ngrok": "./node_modules/.bin/ngrok http -region eu 3000",
    "build": "rm -rf dist && babel src --out-dir dist --source-maps",
    "deploy": "yarn test && yarn build && sls deploy",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "pg": "^7.6.1",
    "ramda": "^0.26.1",
    "ringcentral-chatbot": "^0.3.9",
    "ringcentral-js-concise": "^0.7.4",
    "serverless-http": "^1.8.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.6",
    "@babel/preset-env": "^7.1.6",
    "@babel/register": "^7.0.0",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^23.6.0",
    "dotenv": "^6.1.0",
    "jest": "^23.6.0",
    "ngrok": "^3.1.0",
    "sequelize-cli": "^5.3.0",
    "serverless": "^1.33.2",
    "sqlite3": "^4.0.4",
    "standard": "^12.0.1",
    "yarn-upgrade-all": "^0.5.0"
  },
  "standard": {
    "ignore": [
      "/dist/"
    ]
  },
  "yarn-upgrade-all": {
    "ignore": [
      "babel-core"
    ]
  }
}
